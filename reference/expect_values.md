# expect values

expect values

## Usage

``` r
expect_values(values, weights, delta_names = NULL, ...)
```

## Arguments

- values:

  named list of values.

- weights:

  named numeric of equal length as values.

- delta_names:

  alternative names for binary variables. (used for log)

- ...:

  not used
