# Create a rule used by mip

Create a rule used by mip

## Usage

``` r
mip_rule(a, op, b, rule, type, weight = Inf, ...)
```

## Arguments

- a:

  named vector with coefficients

- op:

  operator in ("\<=", "==", "\>=", "\>", "\<")
